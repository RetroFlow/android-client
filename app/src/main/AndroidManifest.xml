<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.scame.retroflowmvp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".RetroFlowApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".BottomNavigationActivity" />

        <activity android:name=".boards.addedit.BoardAddEditActivity" />

        <activity android:name=".boards.view.BoardViewActivity" />

        <activity android:name=".boards.view.sprints.section.ActionItemsListActivity" />

        <activity android:name=".boards.view.sprints.section.action_item.ActionItemActivity" />

        <activity
            android:name=".entry_point.SplashActivity"
            android:theme="@style/AppTheme.Launcher">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity android:name=".entry_point.login.LoginActivity" />

        <activity android:name=".entry_point.registration.RegistrationActivity" />

    </application>

</manifest>